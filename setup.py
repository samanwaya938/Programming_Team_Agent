# Setup file for crafting a Python package
from setuptools import setup, find_packages
setup(
    name='programming_team_agent',
    version='0.1.0',
    author='Your Name',
    packages=find_packages(),
    install_requires=[]
)